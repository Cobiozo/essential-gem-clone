
# Zmiany w kontaktach prywatnych — ZREALIZOWANE

## 1. Zmiana nazwy "Własna lista" → "Moja lista kontaktów" ✅
## 2. Ukrycie ikony rejestracji na spotkania w zakładce "Moja lista kontaktów" ✅

Dodano prop `hideEventInfo` do `TeamContactAccordion` i `TeamContactsTable`. Przekazywane jako `hideEventInfo={privateSubTab === 'own'}`.
